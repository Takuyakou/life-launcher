# Life Launcher v1.1候補 UI/UX・機能仕様書

## 0. 文書情報

| 項目 | 内容 |
| --- | --- |
| 文書版 | 1.1-rc |
| 対象 | Windowsデスクトップ版 Life Launcher |
| 実装基準 | Public repository `Takuyakou/life-launcher` のPhase 6 stacked candidate |
| 基準コミット | `b2c4a8e86449dcdb67ead7ff5ec46f85973d9f35` |
| 確認日 | 2026-09-07 |
| UI実装 | Tauri 2 / React 18 / TypeScript / CSS |

本書は、Life Launcher v1.1候補のUI/UXと主要機能を、Phase 6のコード、型、設定、capability、テストから整理した現行仕様書である。

Public repository内の `package.json` と `src-tauri/tauri.conf.json` は、release prep前のためバージョン `1.0.0` を保持している。最新機能の入口は [Overview](../OVERVIEW.md) を参照する。

## 1. プロダクト概要

### 1.1 目的

Life Launcherは、迷っているユーザーに「今やる一手」を渡し、必要なアプリ・ファイル・URL・手順書とタイマーを一続きで起動するWindows向け実行支援アプリである。

予定を細かく管理することより、次の流れを短くすることを重視する。

1. 今日の方向を決める。
2. 実行可能な一手を選ぶ。
3. 必要な作業環境を開く。
4. 短時間または通常時間のタイマーを開始する。
5. 実行結果をローカルへ記録する。

### 1.2 中心となる価値

- サイドバーの少数精鋭ランチャーから、よく使う対象をすぐ開ける。
- 辞書ウィンドウから、多数の登録対象を検索して呼び出せる。
- 「今日の勝利条件」「今日の3件」「今やる一手」で判断負荷を下げる。
- プロジェクトの次の一手と作業環境を結び付けられる。
- タイマー記録から、その日の実行内容を自動生成する。
- ローカルのMarkdown、Text、HTML手順書を作業中に参照できる。
- アカウントや自前サーバーを必要とせず、データを端末内に保持する。

### 1.3 設計原則

- 実行開始までの操作数を少なくする。
- 自動確定より、ユーザーが最後に確定する余地を残す。
- 推薦は説明可能な固定ルールだけを使う。
- メイン画面は中程度の情報密度を保つ。
- 補助情報は折りたたみ、辞書、設定、別ウィンドウへ分離する。
- 黄色を意思決定とフォーカスに使い、状態色を意味別に使い分ける。
- Windowsアプリとして、マウス、キーボード、D&D、トレイを一貫して扱う。

## 2. 対応環境とウィンドウ

- 対象OSはWindows。
- メインウィンドウは初期 `1080 x 680`、最小 `860 x 560`、リサイズ可能。
- 多重起動時は新しいプロセスを増やさず、既存のメインウィンドウを復元する。

| ウィンドウ | 用途 | 主な特徴 |
| --- | --- | --- |
| Main | 今日の実行、記録、設定、使い方 | 閉じる操作ではトレイへ格納 |
| Dictionary | ランチャー辞書 | フレームレス。検索、分類、D&D |
| Instruction Viewer | 手順書の閲覧・管理 | Markdown、Text、HTMLを表示 |
| Mini | タイマーの小型表示 | `288 x 136`、常に手前、タスクバー非表示 |

### 2.1 トレイ

- メインの閉じるボタンでは終了せず、通知領域へ格納する。
- 実行中・一時停止中のタイマーは維持する。
- トレイアイコンの左クリックでメインを復元する。
- トレイメニューから「開く」と明示的な「終了」を選べる。

## 3. メイン画面構造

```text
Main Window
├─ Sidebar
│  ├─ Brand
│  ├─ Dictionary launcher
│  ├─ Quick launcher groups
│  └─ Timer panel
└─ Main panel
   ├─ Page header / top toolbar
   ├─ Victory condition
   ├─ Do now
   ├─ Today builder
   ├─ Today 3
   ├─ Project next steps
   ├─ Wishlist
   └─ Today activity
```

- サイドバー幅は `244px`。
- メイン内容幅は最大 `1180px`。
- 最大化時はメイン領域内で中央寄せし、片側だけに余白を偏らせない。
- サイドバーとメイン領域は独立してスクロールする。
- 上部ヘッダーは本文スクロールから分離する。

## 4. ビジュアルシステム

### 4.1 色

| 用途 | 値 |
| --- | --- |
| Background | `#161512` |
| Surface | `#211f1a` |
| Surface strong | `#2b2922` |
| Surface soft | `#1b1a17` |
| Line / soft line | `#4a4639` / `#353126` |
| Text | `#f4f0e8` |
| Muted / readable muted | `#b8b0a0` / `#a39c8e` |
| Accent / strong accent | `#e7b94d` / `#ffce5b` |
| Positive | `#6fcf97` |
| Warning | `#f2a65a` |
| Error | `#f06f68` |

- 黄色は選択、挿入位置、フォーカス、勝利条件。
- 緑は短時間開始、実行中、成功。
- 青は通常タイマー開始。
- 橙は一時停止、赤は終了・削除・破棄。
- プロジェクト色はamber、blue、green、violet、rose、cyan、orange、slate。

### 4.2 文字と形状

- 基本フォントは `Segoe UI`, `Yu Gothic UI`, `Meiryo`, `system-ui`, `sans-serif`。
- ページタイトル約22px、勝利条件・今やる一手約17px、見出し・本文約14px、補足約11px。
- 文字間隔は0を基準とし、負のletter-spacingは使わない。
- 基本角丸は `8px`。
- セクション全体を装飾カード化せず、カードは個別項目と操作面に限定する。
- 長いラベルは省略表示とツールチップで補完する。
- ボタン、カード、ツールバーには安定した寸法を与える。

## 5. 左サイドバー

### 5.1 ブランドと辞書

- 左上にカラーアイコン、`Life Launcher`、`Quick`を表示する。
- 上部に「辞書を開く」と `Ctrl+K` を表示する。
- クリックまたは設定済みショートカットで辞書を開く。

### 5.2 Quickグループとボタン

- `showInSidebar=true` のランチャーだけを表示する。
- グループは折りたたみでき、状態をローカルに保持する。
- 見出しには名称と項目数を表示する。
- グループの右クリックから追加、名称変更、削除を行う。
- グループはD&Dで並べ替えられる。
- ボタンはアイコンとラベルを表示し、クリックまたはフォーカス後のEnter / Spaceでアクションを定義順に実行する。
- グループ見出しはクリックまたはフォーカス後のEnter / Spaceで折りたたみ、`aria-expanded`で状態を公開する。
- ボタンの右クリックから編集、対応するローカル対象のExplorer表示、辞書に移動、削除を行う。辞書に移動しても登録内容とアクションは保持する。
- Explorer表示は単一のlocal file / folder actionだけを対象とし、URL、shell-special、複数actionでは表示しない。
- ボタンは同一グループ内および別グループへD&D移動できる。
- 約6pxの開始しきい値で通常クリックとの誤判定を避ける。
- ドラッグ対象にはゴースト、移動先には黄色の挿入線を表示する。
- ドラッグ中以外は挿入線を表示しない。
- 初期configは特定用途のサンプルを持たない汎用的な空状態。

### 5.3 サイドバータイマー

- 待機中は通常タイマーの分数を表示する。
- `-` / `+` は5分単位、数値入力とスピナーは直接変更。
- 分数表示の上下ドラッグは1分単位。
- 設定範囲は1〜240分。
- 実行中は対象名、残り時間、進捗、一時停止、終了を表示する。
- 一時停止中は再開と終了を表示する。

## 6. 上部ヘッダー

- メイン画面のタイトルは `今日 ・ N分`、隣に月日と曜日を表示する。
- 記録画面では「記録」に切り替える。
- 意味が曖昧だった「通常」バッジは表示しない。

| グループ | 日本語 | 英語 | 役割 |
| --- | --- | --- | --- |
| System | 自動起動 | Startup | Windows自動起動 |
| System | ミニ | Mini | ミニタイマー |
| View | 記録 / メイン | Records / Home | 画面切替 |
| View | 手順書 | Instructions | 手順書ビューアー |
| Help | 使い方 | Guide | アプリ内ガイド |
| Help | 設定 | Settings | 設定ダイアログ |

- アイコンと短い名称を併記する。
- 日本語または英語は実行環境の言語に合わせる。
- ホバー時は背景、境界、文字色を変える。
- 自動起動が有効な場合は緑の状態表現を維持する。
- 狭い幅ではラベルを段階的に隠し、アイコン、ツールチップ、ARIAラベルは残す。

## 7. 今日の勝利条件

- 今日もっとも達成したい状態を1件だけ表示する。
- 金色の枠とトロフィーで優先度を示す。
- チェックボックスで達成状態を切り替える。
- 本文はインライン編集でき、最大90文字。
- 候補チップは入力欄へのセットまでとし、自動確定しない。
- 勝利条件の自動生成、AI提案、達成率表示は行わない。

## 8. 今やる一手

- 説明可能な固定ルールで1件を提示する。
- 今週の重点、当日のセッション有無、更新・確認時刻、手動順序を材料にする。
- 推薦理由を短い補足として表示する。
- AI、利用回数順、非公開スコアを使わない。
- プロジェクト色、名称、次の一手、任意の開始トリガー、通常時間を表示する。
- 14日以上実行がない場合は短時間からの再開を案内する。
- 関連手順書がある場合だけ手順書ボタンを表示する。
- 右側上部に短時間開始と通常開始を横並びで表示する。
- 短時間は緑、通常は青。分数はグローバルまたはプロジェクト設定に連動する。
- 開始時は関連ランチャー、手順書、タイマーを同じ実行経路で扱う。
- 実行アクションがなくても、手順書の開始時表示があればタイマーを開始できる。
- 実行中は一時停止と終了、一時停止中は再開と終了へ切り替える。

## 9. 今日を組み立てる

- 候補からその日の行動を選ぶ折りたたみセクション。
- 「今日の3件」の直後、「次の一手」の前に配置する。
- 候補は「プロジェクトの次の一手」と「やりたいこと」から構成し、勝利条件や最近の記録は候補に含めない。
- 今日の3件が3件の場合は制限を超えて追加しない。
- 見出しや本文に新規登録の追加ボタンを置かず、登録は各候補元の既存導線で行う。
- 候補行の「今日へ」を主な採用操作とし、送付先selectや登録削除は持たない。
- 候補は「次の一手」「やりたいこと」にまとめ、Project名を伴うcompact rowで表示する。
- 候補行のプロジェクト名・行動文の列位置とホバー/フォーカス背景は登録元の行と揃える。右端の「今日へ」操作は維持する。
- 採用済み候補は安定したsource identityで判定し、同じ文面のやりたいことも別項目として扱う。
- 候補はD&Dと右クリックの上下移動で並べ替えられる。
- 約6pxのしきい値、ゴースト、黄色の挿入線で状態を示す。
- 表示順はローカルUI状態として保持する。
- 候補は5件ずつ表示し、6件以上では前後ページへ移動する。
- 候補元の変更で最終ページが空になった場合は、存在するページへ自動で戻す。
- 右クリックの「今日の候補から外す」は、安定したsource identityを当日の除外一覧へ保存し、同じ候補のToday3も外す。元の次の一手・やりたいこと・Sessionは保持する。
- 候補除外は同日reload後も維持し、日付切替で解除する。対象のタイマー実行中はUIとhandlerの両方で拒否し、保存失敗時は候補とToday3をrollbackする。
- 旧dismissデータは削除せず保持するが、新しい当日除外へ移行せず、候補の表示可否にも適用しない。
- 候補が0件の場合は「次の一手」「やりたいこと」の登録元へ移動し、既存の追加ボタンへフォーカスできる。

## 10. 今日の3件

- 当日に取り組む項目は最大3件。
- 見出しの隣に `N件`、`完了数 / 現在件数 完了` の順で表示する。完了表示は全件完了時だけ緑にする。
- 見出しや本文に新規登録の追加ボタンを置かない。0件時は「今日の候補を見る」からBuilderを展開してフォーカスする。
- 3件時は横3列を保ち、常設の開始操作を収めながら過剰な高さと余白を持たないcompact cardで表示する。
- 3件制限を解除する設定や回避経路は持たない。
- 未完了項目を翌日へ自動繰り越ししない。
- チェックボックスと項目名を表示する。
- プロジェクトがある場合、色付きドットと名称を本文の基準線に揃える。
- プロジェクトがあるカードは、上端の細線にも同じプロジェクト色を使う。未設定カードは中立線のままにする。
- 任意の「きっかけ」を持てる。
- きっかけはTimerの直上に配置する。未設定時の「+ きっかけ」は、その専用領域へのホバーまたは領域内のキーボードフォーカスでのみ表示し、カード全体へのホバーでは表示しない。設定済みは同じ位置に常時表示する。領域の高さを確保し、表示時にTimerやカードが動かないようにする。
- 最終行に重複した罫線を表示しない。
- カード左下のghost actionと右クリックの「今日の3件から外す」で、対象のToday3採用だけを解除する。確認ダイアログは出さず、保存成功後に「今日の3件から外しました」と通知する。
- 元の次の一手・やりたいこと、Builder候補、Sessionと今日の実行は保持する。sourceの完了・削除や当日の候補除外にはしない。
- 対象Today3をsourceとするタイマーが実行中・一時停止中・満了確認中はUIとhandlerで解除を拒否する。他のToday3のタイマーは解除を妨げない。保存失敗時は既存の保存契約でsnapshotと表示順をrollbackする。
- 完了済みも従来の右クリック解除と同じく対象とする。「次の3件を選ぶ」は引き続き3件全完了時のみ表示し、1件外した場合は残る完了状態を保ったまま空き枠へ再採用できる。
- 短時間開始と通常開始を常時表示する。
- ボタン幅は2桁以上の分数でも崩れない固定寸法。
- 初期状態は再生アイコン、ホバー時は分数を表示する。
- 短時間は緑、通常は青。
- 実行中は一時停止と終了、一時停止中は再開と終了へ切り替える。
- プロジェクト、ランチャー、手順書が紐づく場合は開始時に同じ実行環境を利用する。
- 採用時に項目名、きっかけ、開始環境、手順書、短時間・通常タイマー分数をToday項目へ保存する。元の登録を明示編集保存した場合は現在のToday3も再snapshotする。常時live bindingではない。
- 完了は予定時間到達後の確定、または動的な早期終了確認で「今日の分は完了」を選んだ場合に付与する。
- 1分未満、早期基準未満、または「未完了のまま終了」では今日の項目を完了扱いにしない。
- 3件すべてが完了した場合だけ「次の3件を選ぶ」を表示する。手動操作で現在の枠を空にし、Builderを展開してフォーカスする。

## 11. プロジェクトの次の一手

### Today3との編集同期

- canonicalはNextStep(Project.id) / Wishlist(stable inbox.id)。Today3右クリック「編集」は元editorを開く。曖昧な旧aliasや元データなしは編集不可。
- 明示保存時だけ、text/trigger/projectId/buttonIds/instructionPath/instructionOpenOnStart/defaultTimerMinutes/shortTimerMinutesを再snapshotする。解除した任意値は残さない。[詳細matrix](../phase7.1/00-field-sync-matrix.md)。
- 現在のToday3のsourceKey、done、配列順、個数、date、勝利条件は保持する。完了済みの現在カードもdoneを維持して更新し、Session/今日の実行/sourceCompletionsは書き換えない。部分補充なし。
- Project編集はそのProject sourceだけ更新し、同じprojectIdを持つ別Wishlistは自動更新しない。Wishlist自身の編集保存時に関連Projectの現在の分数を解決する。
- 同sourceのrunning/paused/満了確認/早期終了確認中はUI・直接handler双方で拒否する。別source Timerは編集を妨げない。保存中は同source開始も拒否し、実行中の分数・開始環境を変更しない。
- sourceとToday3を一つのconfigとして保存し、成功後だけeditorを閉じる。失敗時は既存rollbackにより表示を戻しdraftを残す。ファイル置換のcrash atomic性と他writer競合の制約は監査文書を参照。
- Project/Wishlist追加・編集footerは左保存、右キャンセル。初期入力focusは既存を維持。Start Environment Pickerは左選択を反映、右キャンセルで初期focusも右。Pickerの反映は親draftのみ変更する。
- Project/SettingsのTimer入力順は短時間→通常。狭幅は同じ順で縦積み。分数の単位・継承・validation・増減操作は変更しない。
- 早期完了基準は有効なToday3短時間snapshotと5分の小さい方。整数1..240以外は5分fallback。基準以上・予定時間未満の手動終了だけ確認し、予定時間到達は満了が優先する。停止中編集後の新snapshotは次回から適用し、実行中には変えない。

- 次回すぐ再開する内容を、プロジェクト色・名称・次の一手・任意の開始トリガーを持つcompact rowで表示する。
- このセクションには短時間・通常タイマー、完了チェック、常設編集ボタン、常設「今日へ」ボタンを置かない。
- 今日の3件への採用は「今日を組み立てる」の「今日へ」を主経路とし、行の右クリックにある「今日へ」からも行える。どちらも共通の3件上限とsource identity判定を使い、元のプロジェクトを残す。
- 見出しの隣に「プロジェクトを追加」ボタンと件数を表示し、追加時はプロジェクト名、次の一手、開始条件、手順書、タイマー、開始環境を設定できる詳細ダイアログを使う。
- 行はD&Dで並べ替えでき、ゴーストと挿入位置を区別する。
- 右クリックから今日へ、上へ移動、下へ移動、編集、完了にする、削除を行える。
- 「完了にする」はProjectを残して次の一手・trigger・鮮度情報を空にし、完了snapshotを作る。「削除」はProject登録自体を削除し、完了snapshotを作らない。どちらも同じ候補のToday3と当日除外を解除する。
- active timer対象の完了と削除はUIとhandlerで拒否する。保存失敗時は登録・Today3・履歴を一体でrollbackする。
- 編集では開始環境Pickerからサイドバー・辞書の登録を検索し、最大2件まで選べる。保存済み3件以上は切り捨てず、2件以下へ減らすまで新規追加だけを拒否する。ID配列順を起動順として保持する。
- プロジェクトごとの通常時間、短時間、開始メモ、手順書、開始時の手順書表示は今やる一手または今日の3件から開始する際に使う。

## 12. やりたいこと

- 後で整理する内容の一時置き場で、初期状態は折りたたむ。
- 見出し補足は「あとで整理する一時置き場」。
- 各項目は後方互換な安定IDを持つ。旧データで欠けているIDは読み込み時に一度だけ補完して保存する。
- 小さな追加ボタンと件数は見出し枠内に置く。
- 展開本文には常設の追加行を置かない。見出しの追加ボタンから、本文だけを登録する小型ダイアログを開く。
- 登録ダイアログは初期フォーカス、Tab循環、IME変換中のEnter抑止、空入力と120文字上限、保存中の多重送信抑止を備える。
- キャンセル、Escape、外側クリックは未保存入力を破棄して起点へフォーカスを戻す。保存失敗時は入力を保持し、ダイアログ内で再試行できる。
- 項目名は今日の3件より少し抑えた濃度で可読性を保つ。
- D&Dで並べ替え、ゴーストと黄色挿入線を表示する。
- 右クリックから上へ移動、下へ移動、編集、完了にする、削除を行う。完了は項目を一覧から外してsnapshotを作り、削除はsnapshotを作らない。どちらも確認を必須とする。
- 編集は専用ダイアログで行う。
- 本文、プロジェクト、開始環境、手順書、開始時の手順書表示を設定できる。開始環境はProject編集と共通の検索Pickerを使う。
- プロジェクト設定時は、次の一手と同じ濃度・サイズで色と名称を表示する。
- 各項目は次の一手と同じcompact rowとし、左にプロジェクト名、中央に行動文、右に操作メニューを配置する。未設定は「プロジェクトなし」と表示する。
- 両セクションの見出しは名称・追加・件数・説明の順とし、やりたいことの一覧に装飾の左線は表示しない。
- 今日を組み立てる・次の一手・やりたいことの行は最小48px、上下余白5pxで揃える。候補内のグループ見出しは「今日の3件」と同じmuted色・太さで、1px小さい13pxとする。
- 行のホバーまたはキーボードフォーカス時に背景を強調し、右端の「…」操作を表示する。クリックで既存の右クリックメニューを開き、D&Dは開始しない。タッチ環境では操作を常時表示する。
- 今日の3件への採用は「今日を組み立てる」から行い、3件上限を超えない。

## 13. 今日の実行

- 当日のセッションから自動生成する。
- 手入力の「今日できたこと」入力欄は持たない。
- 初期状態は折りたたむ。
- 見出し補足は「タイマーで実行した内容」。
- 緑の「自動」表示と件数を重ならないように表示する。
- 行には開始時刻、プロジェクト色、名称、実行名、分数を表示する。
- セッションnoteを分類・タグ化しない。
- セッション追加、編集、削除、復元、ホットリロード後に再計算する。
- 件数上限は設けない。

## 14. タイマー

```text
待機中
  └─ 開始 → 実行中
              ├─ 一時停止 → 一時停止中
              │                ├─ 再開 → 実行中
              │                └─ 終了 → セッション確定
              ├─ 終了 → セッション確定
              └─ 満了 → 完了通知 + セッション確定
```

- アクティブなタイマーは常に1本だけ。
- 実行中または一時停止中に別タイマーを開始すると、古いタイマーを終了扱いで確定する。
- 古い項目の実行中・一時停止表示を解除してから新しいタイマーを開始する。
- 一時停止時間は実行分数へ含めない。
- 手動終了、切り替え、満了確認での終了時にセッションを確定する。早期完了の確認待ち時間は含めない。
- 1分未満の実行は通常のセッション記録へ追加しない。
- 満了後は15分の延長導線を持つ。
- noteは自由テキストのまま保存する。
- 開始・一時停止時は右下にトーストを表示する。
- 複数トーストは重ならず積み、右から入り右へ消える。
- 満了時はメインを復元し、OS通知と完了ダイアログを表示する。

### 動的な早期完了

- 対象は未完了Today3に一意に対応するTimerの手動終了。`elapsedSeconds >= thresholdSeconds` かつ `elapsedSeconds < activeTimer.targetMinutes * 60` で確認する。
- `thresholdSeconds = Math.min(5, validSnapshotOr5) * 60`。現在の `TodayItem.shortTimerMinutes` snapshotを使用し、Project値を直接参照しない。停止中の明示編集保存ではP71の同期契約に従いsnapshotが更新され、次回開始に適用される。
- 有効なsnapshotは整数1..240分。runtimeのmissing/null/小数/範囲外/非数値は5分fallback。既存の読込時補完は維持する。
- 短時間3分なら2:59では確認なし、3:00から対象。短時間10分でも5:00から対象。ただし予定時間到達は満了フローが優先し、予定3分のTimerが3:00になった場合は早期確認ではない。
- 一時停止時間を除いた実効経過秒を停止要求時に固定し、確認待ちは除外する。Session分数は切り捨て、1分未満は記録しない。延長後は現在の予定時間で判定する。
- タイトルは「今日の分は完了にしますか？」。左「今日の分は完了」、右「未完了のまま終了」で初期focusは右。Escape/閉じるは右と同じ、backdropでは閉じない。IME確定では送信しない。
- 左右ともSessionを保存し、左だけ対象Today3のdoneを更新する。NextStep/Wishlist本体とsource完了履歴は変更しない。
- Today直接source identityを優先する。今やる一手は対応する採用済みProject項目がある場合のみ対象。未採用・対応が曖昧な項目・Timer切替は早期確認の対象外。
- Session保存失敗では停止時点を固定した確認画面を維持し再試行できる。Today保存失敗ではToday3をrollbackし、保存済みSessionを保持する。二重回答・古いTimer handler・確認中の再開/切替はguardする。
- しきい値到達だけの自動完了、80%判定、2/3の部分補充、自動補充はしない。3件完了時の手動「次の3件を選ぶ」を維持する。

### 14.1 ミニウィンドウ

- 固定サイズ `288 x 136`、フレームレス、常に手前、タスクバー非表示。
- 自動表示時にフォーカスを奪わない。
- タイトル領域をドラッグして移動できる。
- ダブルクリックまたは拡大操作でメインへ戻る。
- 最後の位置を保存し、モニター外へ出た場合は補正する。

## 15. 辞書ウィンドウ

### 15.1 基本とロック

- `showInOverlay=true` の登録だけを表示するコマンドパレット。
- 初期 `760 x 520`、最小 `420 x 300`、フレームレス、リサイズ可能。
- 位置とサイズを保存する。開くたびにメインウィンドウがあるモニターを優先し、前回位置が同じモニター内なら維持、別モニターまたは画面外なら対象モニター中央へ移して作業領域内に補正する。
- 辞書表示中はメイン画面を暗くする。
- 閉じるボタンの左にロック切替を置き、初期状態はロック中。
- ロック中は範囲外クリックや他ウィンドウ操作で閉じず、暗転も維持する。
- ロック解除中は範囲外クリックで閉じる。
- ロック解除中でもタイトルバーのドラッグで移動できる。

### 15.2 ページ、検索、タイル

- 固定ページ「すべて」「未分類」とユーザー作成ページを表示する。
- 選択中ページだけを選択状態にする。
- 固定ページは並べ替え対象外、ユーザーページはD&Dで左右へ移動できる。
- ラベル、グループ、ページ名、aliases、説明を検索対象にする。
- 最近使ったもの、おすすめ、使用回数順は持たない。
- 登録数にかかわらずタイル寸法を共通化する。
- 長いラベルは省略し、titleで全文を補完する。
- タイルは同一ページ内と別ページ間でD&D移動できる。
- ゴーストはポインターから約10pxずらす。
- 別ページタブへ一定時間ホバーするとページを切り替える。
- タイルはLeft / Rightで同じvisual row、Up / Downで最寄りのvisual rowへfocusを移す。
- 辞書を開いた直後は最初のタイルへfocusする。辞書ウィンドウがfocus中かつ入力・タブ・ボタン操作中でなければ、余白からも選択中タイルの矢印移動を続けられる。
- 検索入力中の矢印キーと、メイン画面・他アプリのキー操作は奪わない。文字入力は検索欄へ送る。
- 最上段タイルのUpは選択中categoryへ戻り、categoryのLeft / Rightは選択を変えずfocusだけを移す。
- categoryはEnter / Spaceで選択し、Downで最寄りのタイルへ移動する。
- 検索入力中の矢印キーは通常のeditable cursor操作を優先する。
- 選択中categoryはhoverでもsemantic fillを維持し、focus-visibleはfillを変えず外側へringを重ねる。

### 15.3 右クリック

- 移動、サイドバーに移動、編集、対応するローカル対象のExplorer表示、削除を行う。
- 「サイドバーに移動」は対象項目単位で有効状態を判定し、すでに表示中ならdisabledにする。
- サイドバーに移動しても辞書登録、アクション、辞書内の表示順を保持する。
- Sidebar側の「辞書に移動」はQuick表示だけを外し、辞書登録とアクションを保持する。
- 双方向の移動保存に失敗した場合は表示状態をrollbackする。
- 完了メッセージは約3秒で消える。
- メニューは最終位置へ即時配置し、横から滑り込まない。
- 画面端では内側へ補正する。
- `role=menu`、キーボード操作、Escape終了に対応する。
- focus中のタイルはShift+F10またはMenu keyで同じmenuを開く。

## 16. ランチャー登録と編集

### 16.1 対応アクション

1つのボタンは複数アクションを持ち、登録順に実行できる。

- アプリ、URL、フォルダー、ファイルを開く。
- スクリプトを引数付きで実行する。
- Windowsのごみ箱を開く。

URLはWindowsの既定ブラウザで開き、登録ごとのブラウザ指定は持たない。

### 16.2 外部D&D

- アプリ、フォルダー、ファイル、Windowsショートカット、URL、ごみ箱を受け付ける。
- ブラウザのタブ、アドレス、ブックマークURLも受け付ける。
- `.lnk` はリンク先を解決する。
- Chromeブックマークは、利用可能な場合にアクティブプロファイルから名称を得る。
- 複数項目は先頭を登録候補にする。
- ドロップだけで起動せず、確認ダイアログを経て保存する。
- 確認画面ではラベル、グループ、サイドバー、辞書、辞書ページを選ぶ。

### 16.3 アイコン

- ローカル対象はWindows Shell由来のアイコンを非同期取得する。
- ショートカット矢印を合成せず、リンク先アイコンを優先する。
- URLは対象ページまたは `/favicon.ico` から非同期取得する。
- 第三者favicon APIは使わない。
- 失敗時はUIを停止せず、種別フォールバックを使う。
- メンテナンスからキャッシュを再生成できる。

## 17. 手順書ビューアー

### 17.1 ウィンドウと左ペイン

- 初期 `960 x 680`、最小 `720 x 480`、リサイズ可能。
- 状態を保存し、モニター外を補正する。
- 常に手前は既定で有効。
- 自動表示時はフォーカスを奪わない。
- 閉じる操作は手順書ウィンドウだけを破棄する。
- 左ペインは約248px。
- 検索欄は「検索」、ほかに「読み込み」と更新を置く。
- 上部の常設「新規」ボタンは表示しない。
- 読み込みルートは最大5件で、再起動後も保持する。
- フォルダー名変更時は保存した識別情報から可能な範囲で追跡する。
- フォルダー選択ダイアログは完了まで前面に保持する。

### 17.2 対応形式

| 形式 | 表示 | 編集 | アイコン色 |
| --- | --- | --- | --- |
| Markdown | サニタイズ済みHTML | プレーンテキスト | 黄系Book |
| Text | 改行保持テキスト | プレーンテキスト | 青系FileText |
| HTML | サンドボックスiframe | 読み取り専用 | 橙系FileCode |
| Folder | ツリー階層 | 管理操作 | フォルダー色 |

- HTMLはインラインCSSを保持したサニタイズ済み `srcDoc` を使う。
- iframeは `sandbox="allow-same-origin"`。
- script、form、入れ子iframe、object、外部リソース等を除去する。
- HTTP/HTTPSリンクは外部ブラウザで開く。

### 17.3 ツリーと編集安全性

- 任意の深さの階層を表示する。
- 同一親内でD&D表示順を変更し、ローカルに保持する。
- ファイルとフォルダーの右クリックメニューを持つ。
- ファイルの「エクスプローラーで表示」は含有フォルダーを開く。
- 削除はWindowsのごみ箱を使い、永久削除へフォールバックしない。
- キーボード操作とARIA tree semanticsに対応する。
- MarkdownとTextは明示的に編集開始し、保存または破棄する。
- 保存は一時ファイルと置換によるアトミック経路。
- mtime競合を検出し、外部変更を約2.5秒間隔で確認する。
- 登録ルート外、UNC、パストラバーサル、危険なリンク構造を拒否する。
- プロジェクト、次の一手、やりたいことへ手順書を紐づけられる。

## 18. 記録ビュー

- 今日と今週の合計時間。
- 今週のプロジェクト別時間。
- 全期間のプロジェクト別活動日数と合計分数。
- 直近セッション一覧。
- 前週の合計、活動日数、プロジェクト別時間、セッション数。
- 動かしたプロジェクト、今週の重点、鮮度レビュー、完了した項目には、対象範囲を示す短い説明を見出しと並べて表示する。
- 「完了した項目」は明示的に「完了にする」で今後の候補から外した次の一手・やりたいことのimmutable snapshotを新しい順に表示する。SessionとToday3完了とは別履歴とする。
- 週次レビューは固定された事実だけを表示する。
- ランキング、前週比評価、達成率、ストリーク、グラフは表示しない。
- 今日、今週、全期間、プロジェクト、テキストで絞り込める。
- セッションを手動追加、編集、削除できる。
- noteは自由テキストのまま扱い、タグ抽出、分類、集計を行わない。
- 旧形式noteは互換表示する。
- 今日の活動ログをクリップボードへ出力できる。
- 出力には合計、プロジェクト別・項目別分数、時刻、作業名、noteを含める。
- 外部サービスへ自動送信しない。

## 19. 設定

タブは「基本」「ショートカット」「手順書」「バックアップ」「メンテナンス」。

| 基本設定 | 既定値 | UI |
| --- | --- | --- |
| 通常タイマー | 25分 | 入力、スピナー、上下ドラッグ |
| 短時間タイマー | 5分 | 入力、スピナー、上下ドラッグ |
| 1日の開始 | 4時 | 入力、スピナー、上下ドラッグ |
| 常に手前 | OFF | チェック |
| Windows自動起動 | OFF | チェック |
| ミニモード | ON | チェック |
| 今週の重点 | 未設定 | 最大3件 |

| ショートカット | 既定値 |
| --- | --- |
| メイン画面 | `Alt+Space` |
| 辞書 | `Ctrl+K` |
| ミニ | 未設定 |
| 手順書 | 未設定 |

- ショートカットは登録ボタンを押した後のキー入力を記録する。
- 記録中は既存ショートカットを一時解除する。
- 重複や使用中キーを検証し、警告する。
- 登録失敗で設定全体の読み込みを失敗させない。
- 保存完了まで本登録しない。
- 未保存で閉じる場合は確認ダイアログを出す。
- 「編集を続ける」と「破棄して閉じる」は同じ文字サイズと高さにする。

### 19.1 バックアップとメンテナンス

- バックアップ先と保持数を設定でき、既定保持数は30。
- 設定済みの場合、1日1回 `config.json`、`sessions.jsonl`、`notes.json` をZIP保存する。
- ZIPを選択して復元し、復元前に現在データを退避する。
- ZIPの名前、エントリー、サイズ、圧縮形式、チェックサムを検証する。
- データフォルダー、バックアップを開ける。
- アイコン再生成、今日の活動ログコピーを実行できる。

## 20. アプリ内ガイド

- メイン上部の「使い方」から、オフラインで読める静的な日本語Guideを開く。
- Guideは「今やる一手」から始める経路と、ProjectまたはWishlistへ登録し、Builderの「今日へ」でToday3へ採用して開始する経路を区別して説明する。
- Builder候補はProject.nextStepとWishlistだけで、5件ずつ表示し、新規追加や送付先selectを持たないことを案内する。当日だけ候補から外す操作と、登録の完了・削除は区別する。
- Today3は一度に最大3件で、満了確定または早期終了時の明示選択で完了し、3件完了後は手動で「次の3件を選ぶ」ことを説明する。
- Wishlistの追加は本文専用の小型ダイアログ、Projectの追加は詳細Project登録であることを実画面ラベルに合わせる。
- 目次から各節へ移動でき、移動先見出しへfocusする。狭幅では目次を開閉でき、ダイアログ内でTabを循環する。
- 公開済みREADMEやReleaseへ未公開版の操作説明を先行反映しない。

## 21. フィードバックとアクセシビリティ

- トーストは右下へ表示し、最大幅は約420px。
- 複数トーストは重ならず積む。
- 成功、情報、警告、エラーを色とアイコンで区別する。
- ダイアログは安全側へ初期フォーカスを置き、Tabを内部へ閉じ込める。
- 標準 `alert`、`confirm`、`prompt` や `tauri.localhost` を製品UIへ露出させない。
- コンテキストメニューは即時配置し、画面外を補正し、横から滑り込まない。
- 主要アイコンボタンにはARIAラベルとツールチップを付ける。
- フォーカスリングは2pxのアクセント色。
- クリック可能領域はポインターカーソル。
- 辞書、メニュー、手順書ツリーをキーボードで操作できる。
- スキップリンクを持つ。
- `prefers-reduced-motion` を尊重する。
- 色だけに依存せず、文字、アイコン、境界を併用する。

## 22. データと日付

| ファイル / フォルダー | 用途 |
| --- | --- |
| `config.json` | ランチャー、プロジェクト、今日、当日の候補除外、やりたいこと、完了項目、設定 |
| `config.schema.json` | configのJSON Schema |
| `sessions.jsonl` | タイマーと手動追加のセッション |
| `notes.json` | 旧形式互換データ |
| `icons/` | アイコンキャッシュ |
| `backups/` | 更新前退避 |

- configの現行データ版は `2`。
- UIからのconfig書き込みはRustのアトミック保存経路へ統一する。
- 外部変更はホットリロードする。
- 既定の1日開始時刻は4時。
- `date_key_at` と `today_date` を共通の日付判定に使う。
- 開始時刻より前は前日扱い。
- 今日、セッション、記録、日次バックアップで同じ境界を共有する。
- 日付変更時は今日の項目を初期化し、未完了を自動繰り越ししない。

## 22. ネットワークと権限

### 22.1 favicon取得

- HTTP/HTTPSだけを許可し、資格情報入りURLを拒否する。
- localhost、loopback、link-local、private、特殊用途IPv4/IPv6を拒否する。
- DNS解決結果を検査し、検査した宛先へ接続を固定する。
- プロキシを介さず、リダイレクトは最大5回。
- 接続3秒、1リクエスト6秒、全体15秒のタイムアウト。
- HTML最大512KiB、画像最大2MiB、候補最大8件。
- Content-Typeを検査し、画像はデコード後に保存する。

### 22.2 Tauri capability

| ウィンドウ | 主な許可 |
| --- | --- |
| Main | 基本API、補助Webview、表示、フォーカス、位置・サイズ、常時手前、自動起動、状態保存 |
| Dictionary | 基本API、非表示、フォーカス、ドラッグ、リサイズ、状態保存 |
| Instruction | 基本API、破棄、常時手前切替 |
| Mini | 基本API、ウィンドウドラッグ |

- 補助ウィンドウへMain専用権限を付与しない。
- capability契約をRustテストで確認する。

## 23. レスポンシブ動作

- 最小幅内でサイドバーと主要操作を維持する。
- 上部ラベルを段階的に省略しても、アイコンと操作は残す。
- プロジェクトカードは自動折り返しする。
- 長い日本語・英語は省略または改行し、隣接操作へ重ならない。
- 辞書と手順書は個別の最小サイズと内部スクロールを持つ。
- ビューポート幅で文字サイズを連続拡大縮小しない。

## 24. 明示的な非目標

- カレンダー、ヒートマップ、ストリーク、達成率、ランキング、統計グラフ。
- 未完了タスクの自動繰り越し、今日の3件制限解除。
- 期限、優先度、サブタスク、一般的なタグ管理。
- アカウント、ログイン、自前サーバー、クラウド同期、スマートフォン連携。
- AI提案、自動分類、自動確定。
- 勝利条件や次の一手の自動セット。
- 辞書の最近使ったもの、おすすめ、使用回数順。
- noteのタグ抽出、分類、集計。
- URLごとのブラウザ指定。
- 活動ログの自動送信、週次重点の自動リセット。

## 25. 主要な不変条件

1. アクティブまたは一時停止中のタイマーは1本だけ。
2. 今日の3件は最大3件。
3. サジェストは入力欄へのセットまでで、確定はユーザー操作。
4. configのUI書き込みは既存のアトミック保存経路を使う。
5. 日付判定はRustの共通関数へ集約する。
6. ミニウィンドウの自動表示はフォーカスを奪わない。
7. ドロップ対象を確認なしで起動しない。
8. 手順書削除はごみ箱を使い、永久削除へ自動移行しない。
9. セッションnoteは自由テキストのまま保持する。
10. 推薦は説明可能な固定ルールだけを使う。

## 26. 現行制限

- UIは日本語中心で、英語対応は主に上部ツールバー等の一部表示。
- URLはWindowsの既定ブラウザで開く。
- favicon取得失敗時はフォールバックアイコンになる。
- Chromeブックマーク名取得はローカルプロファイルを読める場合に限る。
- HTML手順書は安全性のためスクリプトや外部リソースを除去し、通常ブラウザと完全同一ではない。
- 手順書フォルダー追跡はローカルドライブ上の安全なパスを前提とする。
- `notes.json` は互換目的で残るが、「今日の実行」は `sessions.jsonl` から生成する。
- アプリ内自動アップデーターは現行仕様に含まれない。

## 27. Web Demo companion

- Web Demoは別repository `Takuyakou/life-launcher-web`で管理する静的SPAであり、Windows製品版の完全移植ではない。
- Demoの中心フローは、Project NextStepまたはWishlistへ候補を登録し、Today Builderの`今日へ`で最大3件を選び、Today3から実行する。
- `今日へ`はBuilderだけに置き、登録元のProject / Wishlistへ重複配置しない。
- 候補とToday3はstable source IDで対応し、同じ文面のWishlistも別項目として扱う。
- 候補除外は元sourceを保持したままmatching Today snapshotを外し、localStorageへ保存する。既存schema v2は読み込み時に後方互換補完する。
- native Explorer、実ランチャー起動、Tauri権限、Rust推薦はDemoへ再実装しない。
- backend、認証、analytics、外部APIを使用せず、入力はブラウザのlocalStorageだけに保存する。

## 28. 仕様確認の主要ソース

- `src/App.tsx`
- `src/components/InstructionViewer.tsx`
- `src/components/InstructionTree.tsx`
- `src/dictionaryWindow.ts`
- `src/types.ts`
- `src/styles.css`
- `src-tauri/src/models.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/src/commands/config.rs`
- `src-tauri/src/commands/sessions.rs`
- `src-tauri/src/commands/drop.rs`
- `src-tauri/src/commands/explorer.rs`
- `src-tauri/src/commands/icons.rs`
- `src-tauri/src/commands/instructions.rs`
- `src-tauri/tauri.conf.json`
- `src-tauri/capabilities/*.json`
- `tests/visual/phase6-main.spec.ts`
- `tests/visual/phase6-dictionary.spec.ts`
- `tests/visual/phase6-readiness.spec.ts`
- `tests/visual/phase62-today-layer.spec.ts`
- `tests/visual/phase62-source-lifecycle.spec.ts`
- `tests/visual/phase62-quick-picker.spec.ts`
- `tests/visual/phase62-records-guide.spec.ts`
- `docs/v1.3.0-ui-ux-spec.md`
- `docs/ui-audit/UI-UX-MASTER-AUDIT.md`
- P1.40 / P1.50以降の各作業報告書

計画書や過去の記憶ではなく、Phase 6候補の実装と自動テストを優先して記述している。
