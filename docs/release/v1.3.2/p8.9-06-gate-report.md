# P8.9-06 v1.3.2 Final Gate

- Date: 2026-09-23 (JST)
- Application source commit: `1633a945b458a835e7dd4be6a25234ee7a9dd6e3`
- Product version: 1.3.2 in npm package/lock, Cargo package/lock, and Tauri config. Config schema remains 3.

## Post-version checks

- `npm ci --offline`: PASS (172 packages; 0 npm vulnerabilities).
- `npm run lint`: PASS.
- `npm run build`: PASS.
- Full Playwright suite after the final documentation fix: 413 passed, 48 skipped, 0 failed (461 total). The first run caught a 101-line Overview against its 100-line contract; the redundant line was removed, the focused test passed 3/3, and the full suite was rerun successfully.
- `cargo fmt --check`, `cargo check`, `cargo clippy --all-targets -- -D warnings`: PASS.
- `cargo test`: 129 library tests and 2 capability-contract tests passed.
- `npm audit --audit-level=low`: 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock`: 0 vulnerabilities, 8 existing allowed maintenance/unsoundness warnings. No dependency changed beyond the root package version.
- `npm run public:check`: PASS, 399 source files scanned, 0 blockers. Review EXEs and generated diffs were preserved outside the public source worktree.
- `git diff --check` and staged equivalent: PASS.

## Official candidate

Generated by `npm run package:windows:rc` from the clean source commit. `release-candidate/v1.3.2` has exactly the three release binaries, `SHA256SUMS.txt`, and a byte-identical copy of `docs/releases/v1.3.2.md`.

| File | Bytes | SHA256 |
| --- | ---: | --- |
| `Life-Launcher-v1.3.2-windows-x64-setup.exe` | 3,627,709 | `599B32B7B4317304C3BEC1A1DB79EB72071E5F16C8445D5486F5E68290112B0D` |
| `Life-Launcher-v1.3.2-windows-x64.exe` | 15,930,880 | `B308339377CD58C64D3069608220128505966EFF5E8CB2462A0F357CF7A639DD` |
| `Life-Launcher-v1.3.2-windows-x64-portable.zip` | 5,020,113 | `8998F6D509C49195DA029E69A769ACF513C2811FD032DC671B1B6584A64464FD` |

All three hashes matched an independent recalculation against `SHA256SUMS.txt`. Both EXEs report ProductVersion and FileVersion 1.3.2. The ZIP contains only the same EXE and `README.txt`; no user data. A string scan of both EXEs found no local user/development path, review build path, fixture marker, or private-key marker. Windows Defender real-time protection was enabled, and a custom scan of the candidate found no new threat detection.

## Native smoke and upgrade

- The first standalone attempt was intercepted by a still-running user-owned review EXE. After the user fully closed it, all official candidate formats were retested under separate synthetic `APPDATA`, `LOCALAPPDATA`, `TEMP`, and WebView2 roots. The user process was never stopped by the test.
- Standalone: Main window observed, schema 3 config created, normal exit 0 after 8.6 seconds.
- Portable: extracted EXE hash matched the standalone, Main window observed, schema 3 config created, normal exit 0 after 8.9 seconds.
- NSIS Installer: silent install into a test-only directory exited 0; installed `life-launcher.exe` showed a Main window, created schema 3 config, and exited 0 after 8.9 seconds; silent uninstall exited 0, removed the EXE and uninstall registry entry. A test harness initially looked for the display name `Life Launcher.exe`; the actual installed filename is `life-launcher.exe`. This was corrected before the app and uninstall checks.
- Synthetic v1.3.1 upgrade: the published v1.3.1 standalone hash matched its original SHA256SUMS. It created an isolated schema 3 config with no `mainDisplaySize`. Synthetic Project/NextStep, Today item and victory text, Wishlist item, timer/shortcut settings, and a session file were added. v1.3.2 opened the same profile, exited 0, retained all asserted data and the session hash, and kept schema 3. Config bytes changed only through expected normalization: NextStep timestamps and Today timer defaults were filled in. Missing display size remains absent on disk and defaults to standard in the UI.
- The focused Main display/heading visual suite passed 22/22 after packaging. The full suite covered all three sizes, persistence and Cancel, 620-1920px layout bounds, Main toolbar density, heading alignment, Today Activity indentation, drag geometry, and removal of the Main weekly banner.

## Environment notes

The default `Ctrl+Alt+Space` hotkey is occupied by another process on this host; fresh native profiles log a skipped registration but remain usable and show no save error. Automatic WebView2 teardown logs `Chrome_WidgetWin_0` unregister error 1412, while all candidate processes exit 0. Native OS-level repeated hotkey invocation and click-through of every Main/Settings control were not automated in this final package smoke; those behaviors passed the browser and Rust suites. No user data was opened or modified. No release blocker was observed.
