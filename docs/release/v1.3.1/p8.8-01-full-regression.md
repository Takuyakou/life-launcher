# P8.8-01 Full Regression / v1.3.1 Gate

- Date: 2026-09-23 (JST)
- Decision: PASS for automated full regression; native global shortcut repetition is carried to P8.8-04.
- Product source baseline: `d5b874a665c3507cb234ffd5dc40103ab32dbdab` (only release documents and one obsolete test assertion changed after it).

## Browser and Rust

| Gate | Final result |
| --- | --- |
| Full non-manual Playwright suite | 390 passed, 48 skipped, 0 failed (438 total). |
| No-folder viewer picker cancellation and selection | 10 passed (both flows repeated 5 times). |
| Focused P8.7 Dictionary, Viewer, HTML, and polish suite | 26 passed. |
| Rust unit and capability contract | 129 unit + 2 capability tests passed. |
| `npm run lint` and `npm run build` | PASS. Existing Vite chunk-size warning only. |
| `cargo fmt --all -- --check`, `cargo check --locked`, `cargo clippy --all-targets --locked -- -D warnings` | PASS. |
| `npm audit --audit-level=low` | PASS; 0 vulnerabilities. |
| `cargo audit` | PASS; 0 vulnerability failures and 8 allowed upstream warnings. |
| `npm run public:check` | PASS; 383 files, 0 blockers. |
| `git diff --check` | PASS. |

The first full run found three failures: two README contract checks when artifact filenames were temporarily generalized during the public copy audit, and one historical layout assertion expecting a 36-58 px badge gap after the intended compact header alignment. The README retains the required v1.3.1 filenames while identifying GitHub Releases as the authority for publication status. The layout assertion now checks the current 8-24 px trailing lane; the isolated checks passed 5/5. The complete suite was rerun afterward and passed 390/390 executed tests.

## Coverage and remaining native checks

The full suite includes Project, NextStep, Wishlist, Today Picker/Today3, source protection, Do Now, timer/completion, Records, Settings, Backup/Restore, Software Reset, Mini, Dictionary, Instruction Viewer, responsive layouts, and visual behavior. Browser tests use public synthetic fixtures.

The folder picker freeze fix was repeated in browser tests. Actual OS-level dictionary shortcut open/hide/reopen and fresh/upgrade startup require native execution and are scheduled for P8.8-04. No user profile data was used or changed.

**Blocker:** none found by the automated gate. **Next Stage:** P8.8-02 public copy audit, followed by packaging and native smoke.
