# P8.8-00 Release Baseline / Feature Freeze Audit

- Date: 2026-09-23 (JST)
- Decision: PASS for P8.8-00. Proceed to P8.8-01 only after this Stage is reviewed.
- Source: `d5b874a665c3507cb234ffd5dc40103ab32dbdab` on `main` and `docs/p87-00-current-state-audit` before this report commit.
- Scope: Windows x64 v1.3.1 release. No feature, UI, data model, schema, or version change in this Stage.

## Repository and release state

| Check | Result |
| --- | --- |
| Local `main` / audit branch | Both at `d5b874a`; clean tracked tree and no untracked files before this report. |
| `origin/main` | `ac661e7434342c647c14d2cf210289e12b8aee67`, 15 commits behind local `main`. Must be reconciled before publication. |
| P8.7 changes | Included in local `main`; no unmerged P8.7 branch. Four other historical, unrelated branches remain unmerged. |
| App version | `1.3.1` in `package.json`, `src-tauri/Cargo.toml`, and `src-tauri/tauri.conf.json`. No bump performed. |
| Config schema | Rust `CONFIG_VERSION = 3`; no release-driven schema change. |
| Packaging | `scripts/package-windows.ps1`, `scripts/package-windows-installer.ps1`, and `scripts/prepare-release-candidate.ps1` present. |
| v1.3.0 | Local annotated tag exists. GitHub Release `v1.3.0` is published, not draft or prerelease. Tag and release unchanged. |
| v1.3.1 | No GitHub Release found. No tag or publication created in this Stage. |

## Implemented P8.7 scope

The following items are present in source and/or covered by the focused P8.7 browser tests. This is an implementation inventory, not a native smoke-test result.

- Dictionary: Main entry book icon and shortcut label, settings, icon size preference, tile/list modes, and direct shortcut configuration.
- Drop Register: group and footer controls, dictionary page assignment, and registration presentation.
- Button Edit: shared group control, existing dictionary page choice, optional search/description, and Save/Cancel layout.
- Instruction Viewer: empty folder action, contextual edit, read-only HTML behavior, registration removal, resizable sidebar, four window size presets including 1920 x 1080, and link-to-next-step UI.
- Guide: beginner-oriented chapters and a three-minute first-use path.

Do not describe an unverified proposal as a shipped feature. Native interaction, upgrade compatibility, and package integrity belong to later Stages.

## Baseline gate

| Command | Result |
| --- | --- |
| `npm ci --offline` | PASS; 172 packages installed; 0 vulnerabilities reported. |
| `npm run lint` | PASS. |
| `npm run build` | PASS; existing Vite chunk-size warning only. |
| Focused Playwright (Dictionary, Instruction Viewer, HTML preview, follow-up polish) | PASS; 26/26 tests. |
| `cargo fmt --all -- --check` | PASS. |
| `cargo check --locked` | PASS. |
| `cargo clippy --all-targets --locked -- -D warnings` | PASS. |
| `cargo test --locked` | PASS; 129 Rust unit and 2 capability tests. |
| `npm audit --audit-level=low` | PASS; 0 vulnerabilities. |
| `cargo audit` | PASS; 0 vulnerability failures, 8 allowed upstream warnings (6 unmaintained, 2 unsound). Review again at the final decision. |
| `npm run public:check` | PASS; 383 files scanned, 0 blockers. |
| `git diff --check` | PASS before report creation. |

No real user data was used for these gates. Browser tests used public fixtures; Rust tests used their isolated test data.

## Carry-forward items

- `docs/release/v1.3.1/FINAL-RELEASE-DECISION.md` predates the latest source. It cites commit `5591644` and a three-size viewer. Its READY claim and artifact hashes are stale; P8.8-03 through P8.8-05 must replace them with results from the final source.
- Local `main` is ahead of `origin/main` by 15 commits. Remote synchronization is required before tagging or publication; no push was made here.
- `cargo audit` warnings are non-failing, but the two unsound advisories should be explicitly considered in the final decision rather than described as zero findings.
- The full browser suite, release package, and native v1.3.0 upgrade smoke remain for P8.8-01, P8.8-03, and P8.8-04 respectively. Web Demo is not a Windows release blocker.

**Blocker:** none for P8.8-00. **Next Stage:** P8.8-01 Full Regression, after Stage review.
