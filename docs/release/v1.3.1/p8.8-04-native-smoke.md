# P8.8-04 Native and v1.3.0 Upgrade Smoke

- Date: 2026-09-23 (JST)
- Application source: `30e80e6b5d48c63d1b0f56a3d16c32509641e5f8`.
- Outcome: **PASS for fresh native startup, package formats, and synthetic v1.3.0 upgrade.** Focused UI coverage and remaining environment limits are below.

## Isolation

Every executable test used a newly created `.local-evaluation/p88-04-*` root with separate `APPDATA`, `LOCALAPPDATA`, `TEMP`, and WebView2 user-data folders. All projects, buttons, pages, Today items, Wishlist entries, and sessions were synthetic. No real Life Launcher data was copied, opened, reset, or modified. Only test-created process IDs were stopped. The installer was silently installed and uninstalled inside a test-created folder after checking that no existing Life Launcher uninstall key was present.

## Final distribution formats

| Format | Native result |
| --- | --- |
| Standalone EXE | Responsive Main window, schema 3 config creation, exit code 0. |
| Portable ZIP | Extracted EXE hash equals standalone; independent responsive Main window, schema 3 config, exit code 0. |
| NSIS Installer | Silent install exit 0; installed v1.3.1 executable started with an independent profile and exited 0; silent uninstall exit 0 and installed EXE removed. |

Each fresh format had a distinct data root. WebView2 Runtime was available on this machine. A host-level `Chrome_WidgetWin_0` unregister diagnostic appeared at automatic exit but the candidate exited with code 0 and left no test-created process running.

## v1.3.0 to v1.3.1

A saved v1.3.0 candidate generated the initial schema 3 configuration in an isolated profile. Synthetic records were then added in the supported config shape: group and dictionary page/order, launcher URL button, Project and NextStep, Today item and victory text, Wishlist item, shortcut settings, and a session JSONL record. The final v1.3.1 standalone started and exited normally with the same isolated profile. The resulting config retained schema 3 and every asserted synthetic record; the session file hash was unchanged. The earlier native UI inspection also displayed the Project, NextStep, Today item, Wishlist, and dictionary button after upgrade. The new Dictionary preferences were absent from old WebView2 storage and defaulted to `auto` icon size and `tile` view.

## Focused native UI

- Final official EXE: an empty Instruction Viewer showed its explanatory copy and **zero** body buttons; the upper `読み込み` action remained visible.
- Final official EXE: Dictionary settings could remain open while Main `使い方` was clicked; both dialogs remained open, confirming the dimmer no longer intercepted Main input.
- Final official EXE: Dictionary view switched to list, saved, and still reported list when settings reopened.
- Same application logic before the final empty-viewer-only change: Dictionary button edit opened from a tile context menu and Cancel closed it; Viewer displayed the no-folder state; the four size steps measured 760x540, 960x680, 1280x840, and 1888x1048 on this monitor (the 1920x1080 preset was clamped to work area); Mini opened as a separate WebView.
- The final full Playwright suite covered Drop Register, Button Edit, NextStep instruction linking, Viewer picker cancellation/selection, size bounds, editing, Guide, and keyboard/focus behavior. The no-folder picker cancellation/selection pair also passed five repetitions each (10/10) on the release baseline.

## Limits and diagnostics

The host's `Ctrl+Alt+Space` is occupied by another process. Fresh candidates logged `shortcut registration skipped during startup: HotKey already registered`; they remained usable and produced no save errors. The isolated upgrade fixture disabled that one conflicting shortcut and retained the `Ctrl+K` launcher setting. OS-level shortcut open/hide/reopen repetition could not be safely run without taking over the existing hotkey. Actual Windows folder-picker cancellation, external-file drag-and-drop, and NextStep linking were not repeated with OS input automation on the final binary; their browser/Rust tests passed. This is a coverage limit, not an observed product failure.

No startup failure, migration failure, data loss, or blocker defect was observed. The native package and synthetic upgrade gates pass; final publication readiness is assessed separately in P8.8-05.
