# P8.5-04 Native Smoke Report

## Verdict

**PASS**

All executable tests used isolated `APPDATA`, `LOCALAPPDATA`, and `TEMP` roots under `.local-evaluation/p85-04`. No real Life Launcher user data was read or changed. Only test-created process IDs were stopped.

## Standalone native smoke

The release-candidate standalone executable was launched with a fresh isolated profile and connected through the local WebView2 debugging endpoint.

- Main window title and fresh empty state: PASS
- Initial schema 3 config creation: PASS
- Project creation and persistence: PASS
- NextStep creation and persistence: PASS
- Today Picker open, outside-click protection, candidate selection, save, and restart persistence: PASS
- Guide dialog: PASS
- Settings dialog: PASS
- Dictionary auxiliary window: PASS
- Mini auxiliary window: PASS
- Instruction Viewer auxiliary window: PASS
- Timer start and stop: PASS
- Restart restored the saved NextStep and Today item: PASS
- Test-created processes were cleaned up: PASS

The initial launch attempt while the user's existing Life Launcher was open exited cleanly with code 0 and did not create a second process. After the user closed that unrelated process, the candidate completed the full standalone smoke above.

## Portable smoke

- ZIP contents remained exactly the executable and `README.txt`.
- Extracted executable hash matched the standalone candidate hash.
- Independent native launch with a fresh isolated profile: PASS
- Fresh Main UI and schema 3 config: PASS
- Test-created process cleanup: PASS

## Installer smoke

- Silent install to an isolated directory: exit code 0
- Installed executable native fresh launch: PASS
- Fresh Main UI and schema 3 config: PASS
- Bundled uninstaller: exit code 0
- Installation directory removed: PASS
- Isolated user-data retention sentinel preserved: PASS

## Reset and restore

The standalone candidate executed Software Reset through its real Settings UI.

- Two-step backup choice and destructive confirmation: PASS
- Old process exited and a new candidate process started: PASS
- Reset config remained schema 3: PASS
- Projects, Today items, Wishlist, and visible UI returned to the fresh state: PASS
- Internal pre-reset snapshot was created: PASS

The pre-reset isolated data was then packaged with the same stored-entry ZIP format used by the application and supplied to the candidate's real `restore_backup` IPC.

- Unsupported ZIP compression was rejected before mutation: PASS
- Supported backup restored schema 3 config: PASS
- Restored Project count 1, Today item count 1, `saveBlocked=false`: PASS
- Reloaded UI displayed the restored Project and NextStep: PASS

## v1.2 to v1.3 compatibility

The checked-in compatibility fixtures required by the audit were used. Results:

- Five `migration_tests`: PASS
- Legacy Wishlist stable-ID migration: PASS
- Same-version overlay migration backup requirement: PASS
- v2 to v3 conversion is idempotent: PASS
- Today snapshots and Wishlist data are preserved: PASS
- Project/NextStep settings and legacy pending settings are separated correctly: PASS
- Invalid/future versions are rejected without partial migration: PASS

Archived executables currently stored under the local `v1.2.0` candidate directory generated schema 3 and therefore were not treated as trustworthy schema 2 fixtures. No synthetic user data or real user profile was substituted for the checked-in fixtures.

## High-risk native and regression coverage

The native checks above were combined with the P8.5-01 full Chromium/Tauri-bridge regression suite and P8.5-02 isolated backend fault-injection suite. Together they cover:

- modal draft/backdrop protection
- Today Picker and Today removal behavior
- NextStep creation and Wishlist promotion
- Project delete guards
- reset, backup, restore, rollback, and shortcut reapplication
- Timer and Mini behavior
- card/button hover, focus, and pressed states
- Do Now candidate switching and Project-color hover

## Multi-window and process behavior

- Candidate primary process remained responsive.
- A second launch exited with code 0.
- Exactly one candidate process remained.
- The second launch exercised the single-instance handler while the primary Main process remained responsive; foreground focus was not measured independently.
- Dictionary, Mini, and Instruction targets were created as separate Tauri WebViews.
- Instruction Viewer closed through its own native close control.
- Software Reset exercised the application's normal exit/restart path.
- Final cleanup found no test-created Life Launcher process remaining.

## Limitations

Native automation ran at the host's current Windows DPI and WebView2 version. It did not repeat every Playwright viewport/DPI matrix against native WebView2; those geometry and interaction matrices remain covered by the full P8.5-01 suite.

## Outcome

P8.5-04 is complete. The Windows release candidate advances to the separate Web Demo parity audit in P8.5-05.

