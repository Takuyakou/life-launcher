# P8.5-02 Reset / Backup / Restore E2E

- 実施日: 2026-09-18
- 対象commit: `44395a0`
- 判定: **PASS**

## Isolation proof

テストプロセスの環境を次の専用root配下へ差し替えた。

- `.local-evaluation/p85-02/APPDATA`
- `.local-evaluation/p85-02/LOCALAPPDATA`
- `.local-evaluation/p85-02/TEMP`
- `.local-evaluation/p85-02/TMP`

Native fixtureも `std::env::temp_dir()/life-launcher-p83-*` 以下へ一意なrootを作り、Drop時に削除する。実ユーザーのAPPDATA、config、sessions、notes、backupは使用していない。

## Native test results

| Filter | Result | Coverage |
|---|---|---|
| `commands::reset::tests::` | 11 passed | fresh state, clean-start workflow, rollback, journal, recovery, checksum, external data |
| `backup` | 7 passed | ZIP generation, entries, icons, pruning, migration backup |
| `restore` | 6 passed | clean restore, pre-restore retreat, legacy normalization, rollback |
| `migration` | 6 passed | v2 to v3, idempotence, stable snapshots, invalid/future rejection |

UI orchestration focused suite:

- `phase83-software-reset.spec.ts`
- `phase83-clean-start.spec.ts`
- `phase81-entry-contracts.spec.ts`
- 24 passed, 0 failed

## Standard backup / restore

- Generated ZIP contains `config.json`, `sessions.jsonl`, `notes.json`, `config.schema.json` and valid flat PNG icon-cache entries.
- Restore into clean isolated APPDATA reproduces config, sessions, notes, schema and icon bytes.
- Existing runtime files and icons are copied to a unique `pre-restore-*` directory before overwrite.
- Reload/restore clears recovered save-blocked state and remains retryable after shortcut-registration failure.

ZIP reader audit:

- central and local header bounds checked
- only stored compression method accepted
- compressed/uncompressed size equality checked
- CRC32 checked
- duplicate entries rejected
- unexpected entries and path traversal names rejected by exact runtime-file allowlist / flat PNG name validator
- malformed/truncated offsets rejected

## Software Reset

### With user backup

Browser orchestration verifies backup-folder selection, backup creation before reset, final confirmation, restart request and later restore selection. Native backup and reset functions are independently exercised against isolated filesystem bytes.

### Without user backup

- internal snapshot created
- canonical fresh config and empty sessions produced
- window state removed
- existing backup ZIP and external instruction file preserved
- first Project / NextStep / Wishlist / Today3 / Session workflow succeeds after restart validation

## Failure injection

| Boundary | Expected | Result |
|---|---|---|
| stage | exact rollback | PASS |
| config commit | exact rollback | PASS |
| sessions commit | exact rollback | PASS |
| notes commit | exact rollback | PASS |
| schema commit | exact rollback | PASS |
| icons commit | exact rollback | PASS |
| autostart commit | exact rollback | PASS |
| fresh-state validation | exact rollback | PASS |
| window-state commit | exact rollback | PASS |
| plugin cleanup | exact rollback | PASS |
| truncated journal tail | last full record recovery | PASS |
| corrupt icon/window snapshot | reject recovery snapshot | PASS |
| localStorage clear/reset failure | restore audited browser values | PASS |

No partial reset remained after injected failure. Recovery persists until explicit acknowledgement. Existing user backup ZIP bytes are not changed.

## Timer guard

- running and paused timers: automated UI PASS
- completion prompt and early-stop prompt: both are included in the shared `softwareResetIsTimerBlocked` guard (`activeTimerRef || completionPromptRef || earlyStopRef`); reset entry and continuation both re-check it

## Compatibility

Existing v2/legacy fixtures migrate to schema v3 idempotently. Source snapshots and stable identity are preserved. No unsupported fixture was fabricated.

## Remaining risk

The browser layer uses a Tauri command mock; packaged-process restart and installer upgrade are intentionally deferred to P8.5-04 native smoke.

## Verdict

**PASS**. No reset/restore mismatch, partial rollback, external-file mutation or migration corruption was found.
