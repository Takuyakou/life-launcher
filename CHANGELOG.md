# Changelog

## 1.3.3 - 2026-09-26

### 改善

- サイドバーの右クリックからファイル・フォルダー・URLを登録できるボタン作成画面を追加。登録元を切り替えても入力途中の内容を保持し、空のグループも表示できるようにした。
- サイドバーからメイン、辞書、設定、メニュー、左下の通常タイマーまでのキーボード操作を改善。今日の勝利条件はEnterで編集・確定、Escで編集を抜けられるようにした。
- 実行中のタイマーをメイン画面内で拡大表示できるようにし、次の一手ごとに自動表示を選べるようにした。

### 修正

- 今やる一手の完了後に同じ候補がすぐ戻る問題と、5分タイマーで「続ける」後に通常タイマーから終了すると完了表示が消える問題を修正。
- 同じ次の一手へのドラッグで「やりたいこと」が重複する問題、サイドバーの登録・右クリック位置、アプリアイコン等の誤登録を修正。
- 日付が変わった後やスリープ復帰後の「今日」の表示、ショートカット競合時の案内、記録画面をスクロールしたときの固定タブ表示を改善。
- 操作中の意図しない文字選択と、タイマーの分数変更後のフォーカス移動を抑えた。

### アップデート時の注意

- 設定データの形式はv1.3.2から変わらない。既存の設定、今日の3件、実行記録を引き継ぐ。
- 更新前にアプリを完全終了し、必要に応じて`%APPDATA%\life-launcher`をバックアップすることを推奨。
- 詳細は [v1.3.3 Release notes](docs/releases/v1.3.3.md) を参照。

## 1.3.2 - 2026-09-23

### 改善

- メイン表示サイズに「標準」「大」「特大」を追加し、文字、カード、操作部、本文幅を段階的に調整できるようにした。
- 大・特大では広い画面の上部メニューも本文に合わせて拡大し、記録画面やサイドバーは従来の寸法を保った。
- 今やる一手と各セクションの見出し軸、今日の実行項目の本文位置を整えた。

### 整理

- メイン画面の週次ふりかえり案内を削除した。記録画面の週次ふりかえり機能は維持した。
- 設定データのschemaは3のまま。旧設定に表示サイズがない場合は「標準」を適用する。
- 詳細は [v1.3.2 Release notes](docs/releases/v1.3.2.md) を参照。

## 1.3.1 - 2026-09-23

### 改善

- 辞書へタイル・リストの表示形式と、自動・小・中・大のアイコンサイズ設定を追加し、titlebarの設定ボタンと右クリックから同じ画面を開けるようにした。
- サイドバーの「辞書を開く」に本のアイコンを追加し、ショートカット表示と起動動作を維持した。
- ドラッグ登録画面で登録対象と表示先を分かりやすく示し、不要な登録前通知をなくした。
- アプリ内の使い方を、最初の3分と毎日の流れを中心にした初心者向け10章へ書き直した。
- ユーザー向け表記を「手順書ビューアー」へ統一した。
- 手順書ビューアーへ「コンパクト」「標準」「大」「特大」の4段階サイズ切替と、ドラッグ・キーボードで調整できる可変サイドバーを追加した。

### 修正

- 手順書フォルダが未登録の状態で、フォルダ選択をキャンセルまたは完了した際に操作が停止する問題を修正した。
- 手順書ビューアーのフォルダ未登録画面と上部の操作を整理し、Markdown・テキストの編集とHTMLの表示専用状態を明確にした。
- 手順書の削除操作を廃止し、PC上のファイルを消さない登録解除へ統一した。
- ドラッグ登録で保存に失敗した場合も入力内容を保持し、再試行またはキャンセルできるようにした。
- タイマー調整時の文字選択を抑止し、次の一手のホバー枠をプロジェクトの色へ揃えた。

### アップデート時の注意

- 設定データの形式はv1.3.0から変わらず、既存の設定、今日の3件、実行記録、辞書登録を引き継ぐ。
- 更新前にアプリを完全終了し、必要に応じて`%APPDATA%\life-launcher`をバックアップすることを推奨。
- 詳細は [v1.3.1 Release notes](docs/releases/v1.3.1.md) を参照。

## 1.3.0 - 2026-09-17

### 追加・改善

- config schema v3でProject metadataとNextStep実行設定を分離し、旧設定を安全に移行。
- Today Builderを「今日やるものを選ぶ」Pickerへ置き換え、選択解除、決定/キャンセル、3件目の自動確定を追加。
- NextStep / WishlistからToday3へのD&D、Today3解除Drop Zone、登録元の選択済み表示を追加。
- 次の一手をWishlistから選ぶ導線、置換時の旧NextStep処理、Projectの安全な削除を追加。
- 未完了Today3が参照する登録元へSource Lockを追加し、完了・採用解除まで破壊的変更を防止。
- 時間を決めず0:00から進める計測Timer、全候補を対象とするDo Now順位、空状態とレスポンシブ表示を改善。
- 設定画面を再構成し、検証済みバックアップ付きソフトウェアリセットと復旧journalを追加。
- 日次ZIPバックアップと復元へアイコンキャッシュを含め、登録元削除後や復元後も取得済みアイコンを保持。
- アプリ内Guideをv1.3.0のPicker、Source Lock、Project削除、計測、バックアップ、リセットへ同期。

### 修正・保守

- リセット・復元後の自動起動更新、グローバルショートカット二重登録、連続復元時のファイル競合を修正。
- Today3追加・解除と勝利条件候補選択で発生する不自然なスクロール移動を抑止。
- Today3、今やる一手、NextStep、WishlistのProject色、ホバー、操作位置、空状態、狭幅表示を統一。
- 今日の勝利条件は再チェック時にも演出し、演出中と演出後で同じチェック付き達成表示を維持。
- Modal / Pickerの範囲外クリック閉じを廃止し、変更がある場合だけ未保存破棄確認を表示。

### アップデート時の注意

- v1.2からの初回起動でconfig schemaを`2`から`3`へ移行する。変換前raw backupを作成し、移行・検証・backup失敗時は元のconfigを保持する。
- 更新前にアプリを完全終了し、必要に応じて`%APPDATA%\life-launcher`をバックアップすることを推奨。
- 詳細は [v1.3.0 Release notes](docs/releases/v1.3.0.md) を参照。

## 1.2.0 - 2026-09-11

### 追加・改善

- 短時間タイマーの基準を満たして通常タイマーを途中停止したとき、「今日の分は完了」または「未完了のまま終了」を選べる動的早期完了を追加。
- Today3、次の一手、やりたいことの編集をstable source identityで同期。採用済みsnapshotを明示的な編集保存時に更新。
- Today3と「今日を組み立てる」へ右クリック・「…」メニューを追加し、編集や候補復帰へ同じ操作で到達可能にした。
- Today3から外す操作と候補除外に、後続の変更を壊さない対象差分Undoを追加。
- 通常Toastを4秒、Undo付きToastを8秒のWarm Rich表示へ統一。ホバー・フォーカス・画面非表示中は残り時間を停止。
- 次の一手・やりたいことの件数に応じて、全表示・5件＋展開・10件単位のページ表示を切り替える一覧制御を追加。
- Today Builder候補からToday3、明示除外した登録元からToday Builderへのcross-section D&Dを追加。
- D&D開始直後から有効な移動先を黄色の枠・背景・案内で表示。案内はoverlay表示とし、画面の高さを変えない。
- 勝利条件、Today3、3件完了、今やる一手に、保存成功後だけ短く表示する完了フィードバックを追加。
- サイドバーの待機中タイマー分数を上下D&Dで変更可能にし、Today3の開始ボタン幅と一覧の密度・ホバー表示を調整。

### 修正・保守

- Timer実行中の同一source編集を、入口と保存handlerの両方で拒否。編集中にTimerが始まった場合も保存しない。
- D&Dはdrop時だけ保存し、日付・source・重複・3件上限・active Timerを再検証。保存失敗時は元の表示へ戻す。
- 早期完了の確認待ちや保存待ちをSession時間へ加算せず、二重停止・二重記録・古いTimer操作を拒否。
- Today3、今日を組み立てる、次の一手、やりたいことの行配置・余白・操作表示を統一。
- アプリ内Guideと現行仕様書をPhase 7.2の操作フローへ同期。

### アップデート時の注意

- config schemaはv1.1と同じ`2`で、v1.1からの追加migrationはない。既存の登録、Today3、Session、完了履歴を引き続き使用する。
- 更新前にアプリを完全終了し、必要に応じて`%APPDATA%\life-launcher`をバックアップすることを推奨。
- 詳細は [v1.2.0 Release notes](docs/releases/v1.2.0.md) を参照。

## 1.1.0 - 2026-09-09

### 追加・改善

- 「今日の3件」をプロジェクト色付きカードへ整理し、3列・2列・1列の表示に対応。
- 今日の項目はタイマー満了後の終了確定で完了。3件すべて完了したときだけ、手動で「次の3件を選ぶ」。
- 「今日の3件から外す」で今日への採用だけを解除。元の次の一手・やりたいこと・候補・実行記録は保持。
- きっかけをTimerの直上へ移動。未設定時はその領域のホバー・フォーカスでのみ表示。
- 「今日を組み立てる」を次の一手・やりたいこと由来の候補へ整理し、5件ずつ表示。「今日へ」で元の登録を残したまま採用。
- Builderの「今日の候補から外す」は当日だけ候補を除外し、同じ候補の今日の項目も解除。元の登録は保持。
- 次の一手・やりたいことの「完了にする」を登録の削除と区別し、記録画面に完了履歴を追加。
- やりたいことの追加を小型ダイアログへ整理。同じ文面の別項目は安定したIDで区別。
- Today3へ採用した時点の本文・手順書・開始環境の参照・タイマー分数を保持。
- 開始環境をサイドバー・辞書から検索して選ぶPickerを追加。新規選択は最大2件、既存の超過登録は保持。
- 辞書の矢印キー移動、Quickと辞書間の登録移動、Explorer表示、D&Dの操作表示を改善。
- 辞書を開く際、Life Launcher本体があるモニターに表示するよう調整。
- 開閉バーのクリック範囲を拡大。確認ダイアログの確定操作を左、キャンセルを右へ統一。
- 記録画面とアプリ内ガイドを現在の操作フローに合わせて更新。

### 修正・保守

- Today3のD&Dで掴んだ項目と黄色の挿入位置を表示。drop時の順序保存と保存失敗時の復元を検証。
- 対象タイマーが動いている間の採用解除や登録の完了・削除をUIと処理側で防止。
- ESLint経由の開発依存`js-yaml`を`4.3.2`へ更新し、GHSA-2883-xcg3-v3hhに対応。

### アップデート時の注意

- v1.0のToday3でsource keyが未設定の項目は、移行時に完了状態が未完了へ戻る。Sessionの実行記録は削除しない。
- 更新前にアプリを完全終了し、`%APPDATA%\life-launcher`を別の場所へバックアップすることを推奨。この移行だけでは、書き換え前の自動バックアップは保証されない。
- 詳細は [v1.1.0 Release notes](docs/releases/v1.1.0.md) を参照。

## 1.0.0

First public release of Life Launcher.

- Quick sidebar and searchable command dictionary
- Daily victory condition, today's three items, and next-step workflow
- Short and normal timers with local session records
- Project totals, daily activity, and local notes
- Markdown, text, and sanitized HTML instruction viewer
- Local-first storage with bounded favicon retrieval and per-window Tauri capabilities
